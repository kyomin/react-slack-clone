import Workspace from '@layouts/Workspace/index';
import React from 'react';

const Channel = () => {
  return (
    <Workspace>
      <div>로그인 축하!</div>
    </Workspace>
  );
};

export default Channel;
